from django.http import path

app_name = "api"
urlpatterns = [
        path("/users/register/",...,...),
        path("/users/login/",...,...),
        path("/posts/create/<int:id>",...,...),
        path("/posts/edit/<int:id>",...,...),
        

        ]
