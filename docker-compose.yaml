services:
  # Baza danych PostgreSQL
  db:
    image: postgres:16-alpine
    container_name: ycom_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=ycom_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    networks:
      - ycom_network
    healthcheck:
      # ZMIANA: Dodaliśmy "-d ycom_db", aby sprawdzać właściwą bazę danych.
      test: ["CMD-SHELL", "pg_isready -U postgres -d ycom_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Backend Django
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ycom_backend
    # ZMIANA: Uproszczona komenda. 
    # Polegamy na manage.py (który poprawiliśmy) i zmiennej DJANGO_SETTINGS_MODULE.
    command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./backend:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    depends_on:
      db:
        condition: service_healthy
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      # Ta zmienna zapewnia, że Django użyje właściwych ustawień
      - DJANGO_SETTINGS_MODULE=project_config.settings
    networks:
      - ycom_network

  # Frontend Next.js
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ycom_frontend
    command: npm run dev
    volumes:
      - ./frontend/app:/app/app
      - ./frontend/lib:/app/lib
      - ./frontend/public:/app/public
      - ./frontend/package.json:/app/package.json
      - ./frontend/package-lock.json:/app/package-lock.json
      - ./frontend/next.config.ts:/app/next.config.ts
      - ./frontend/tsconfig.json:/app/tsconfig.json
      - ./frontend/postcss.config.mjs:/app/postcss.config.mjs
      # Izolujemy node_modules i .next wewnątrz kontenera
      - /app/node_modules
      - /app/.next
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000/api
    depends_on:
      - backend
    networks:
      - ycom_network

volumes:
  postgres_data:
  static_volume:
  media_volume:

networks:
  ycom_network:
    driver: bridge